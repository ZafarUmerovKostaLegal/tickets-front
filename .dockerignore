# Dependencies & build outputs
node_modules
dist
.turbo
.next
coverage
*.tsbuildinfo

# Git / IDE
.git
.gitignore
.vscode
.idea
.cursor

# Local env / logs (compose file must stay available for some hosts)
*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
docker-build-debug.log

# Desktop / mobile native artifacts (can be tens of GB — breaks npm ci with exit 228 / ENOSPC)
src-tauri
src-tauri/target
gen
android
ios
.cargo
.github/workflows/tauri-*

# Tests & reports (not needed for production image)
e2e
test-results
playwright-report
playwright/.cache
blob-report
all-blob-reports

# Misc heavy / unused in nginx image
browser-extension
pdf
*.md
.env*
!/.env.example
