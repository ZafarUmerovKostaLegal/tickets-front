services:
  tickets-front:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    ports:
      - "5173:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped

